<H1> LANDING PAGE </H1>

<h2>LOGO</h2>

<h2><%=link_to 'About', about_path%></h2> 

<h2><%=link_to 'ENTER', option_path%></h2>

 <% if user_signed_in? %>
   <div id="signout"><%= link_to 'Sign out', destroy_user_session_path, method: "delete"%></div>
   <div ><%= current_user.email %></div>
    <% else %>
   <p><%= link_to 'Sign In', new_user_session_path%>
   |
   <%= link_to 'Sign Up', new_user_registration_path%>
   </p>
 <% end %>
 <div>